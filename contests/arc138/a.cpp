#include <iostream>
#include <vector>

int main()
{
    int n, k;
    std::cin >> n >> k;
    int64_t *a_arr = new int64_t[n];
    for (int i = 0; i < n; i++)
    {
        std::cin >> a_arr[i];
    }
}