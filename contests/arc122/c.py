import math


def main():
    alpha = 2 / (math.sqrt(5) + 1)
    n = int(input())
    op_lst = []
    x, y = n, math.floor(n * alpha)
    while True:
        nx, ny = math.floor(n * alpha), y
        


if __name__ == "__main__":
    main()
